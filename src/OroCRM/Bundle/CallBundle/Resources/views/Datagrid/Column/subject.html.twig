{% if value %}
    {% import 'OroUIBundle::macros.html.twig' as UI %}

    {% set callId = record.getValue('id') %}
    {{ UI.clientLink({
        'dataUrl': path('orocrm_call_widget_info', {id : callId}),
        'aCss': 'no-hash',
        'label' : value,
        'widget' : {
            'type' : 'dialog',
            'multiple' : false,
            'options' : {
                'alias': 'call_info_widget_' ~ callId,
                'dialogOptions' : {
                    'title' : value,
                    'allowMaximize': true,
                    'allowMinimize': true,
                    'dblclick': 'maximize',
                    'maximizedHeightDecreaseBy': 'minimize-bar',
                    'width': 600
                }
            }
        },
    }) }}
{% endif %}
