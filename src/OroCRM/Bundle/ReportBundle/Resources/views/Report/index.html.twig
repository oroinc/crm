{% extends 'OroUIBundle:actions:index.html.twig' %}
{% import 'OroUIBundle::macros.html.twig' as UI %}

{% block navButtons %}
    {% if resource_granted('orocrm_report_create') %}
    {{ UI.addButton({
        'path' : path('orocrm_report_create'),
        'title' : 'Create report',
        'label' : 'Create report'
    }) }}
    {% endif %}
{% endblock %}
