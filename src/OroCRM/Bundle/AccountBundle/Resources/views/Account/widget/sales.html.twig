{% import 'OroUIBundle::macros.html.twig' as UI %}
{% set tabs = {} %}

{% set tabs = tabs|merge(
    {
        'orocrm_account_widget_leads': {
            'widgetType': 'block',
            'url': path('orocrm_account_widget_leads', {id: entity.id}),
            'tabTitle': 'orocrm.account.widgets.leads.title'|trans
        }
    }
) %}
{% set tabs = tabs|merge(
    {
        'orocrm_account_widget_opportunities': {
            'widgetType': 'block',
            'url': path('orocrm_account_widget_opportunities', {id: entity.id}),
            'tabTitle': 'orocrm.account.widgets.opportunities.title'|trans
        }
    }
) %}

{% set tabs = tabs|merge(
    {
        'orocrm_account_widget_orders': {
            'widgetType': 'block',
            'url': path('orocrm_account_widget_orders', {id: entity.id}),
            'tabTitle': 'orocrm.account.widgets.account_order.title'|trans
        }
    }
) %}

{{ UI.tabPanel(tabs) }}
