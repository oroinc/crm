{% import 'OroUIBundle::macros.html.twig' as UI %}

{% if resource_granted('orocrm_sales_lead_create') %}
    {{ UI.dropdownItem({
        'path': path('orocrm_sales_lead_create', {'account': entity.id}),
        'iClass': 'icon-phone',
        'title': 'Add'|trans ~ ' ' ~ 'orocrm.sales.lead.entity_label'|trans|lower,
        'label': 'Add'|trans ~ ' ' ~ 'orocrm.sales.lead.entity_label'|trans|lower
    }) }}
{% endif %}
