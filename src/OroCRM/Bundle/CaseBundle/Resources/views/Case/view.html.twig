{% extends 'OroUIBundle:actions:view.html.twig' %}
{% import 'OroUIBundle::macros.html.twig' as UI %}
{% import 'OroEntityConfigBundle::macros.html.twig' as entityConfig %}

{% oro_title_set({params : {"%entity.subject%": entity.subject|default('N/A') }}) %}

{% block navButtons %}
    {% if resource_granted('EDIT', entity) %}
        {{ UI.editButton({
            'path' : path('orocrm_case_update', { id: entity.id }),
            'entity_label': 'orocrm.case.entity_label'|trans
        }) }}
    {% endif %}
    {% if resource_granted('DELETE', entity) %}
        {{ UI.deleteButton({
            'dataUrl': path('orocrm_api_delete_case', {'id': entity.id}),
            'dataRedirect': path('orocrm_case_index'),
            'aCss': 'no-hash remove-button',
            'id': 'btn-remove-user',
            'dataId': entity.id,
            'entity_label': 'orocrm.case.entity_label'|trans
        }) }}
    {% endif %}
{% endblock navButtons %}

{% block pageHeader %}
    {% set breadcrumbs = {
        'entity': entity,
        'indexPath': path('orocrm_case_index'),
        'indexLabel': 'orocrm.case.entity_plural_label'|trans,
        'entityTitle': entity.subject
    } %}
    {{ parent() }}
{% endblock pageHeader %}

{% block content_data %}
    {%- set caseInformation -%}
        <div class="row-fluid form-horizontal">
            <div class="responsive-block">
                {{ UI.renderProperty('orocrm.case.subject.label'|trans, entity.subject) }}
                {{ UI.renderProperty('orocrm.case.description.label'|trans, entity.description) }}
                {{ UI.renderProperty('orocrm.case.origin.label'|trans, entity.origin) }}
                {{ UI.renderProperty('orocrm.case.reportedAt.label'|trans, entity.reportedAt|oro_format_date) }}
                {% if entity.closedAt is not empty %}
                    {{ UI.renderProperty('orocrm.case.closedAt.label'|trans, entity.closedAt|oro_format_date) }}
                {% endif %}

                {%- set owner -%}
                    {%- if entity.owner -%}
                        {% if resource_granted('oro_user_user_view') %}
                            <a href="{{ path('oro_user_view', {'id': entity.owner.id}) }}">
                                {{ entity.owner|oro_format_name|escape }}
                            </a>
                        {% else %}
                            {{ entity.owner|oro_format_name|escape }}
                        {% endif %}
                    {%- endif -%}
                {%- endset -%}
                {{ UI.renderHtmlProperty('orocrm.case.owner.label'|trans, owner) }}

                {%- set reporterUser -%}
                    {%- if entity.reporterUser -%}
                        {% if resource_granted('oro_user_user_view') %}
                            <a href="{{ path('oro_user_view', {'id': entity.reporterUser.id}) }}">
                                {{ entity.reporterUser|oro_format_name|escape }}
                            </a>
                        {% else %}
                            {{ entity.reporterUser|oro_format_name|escape }}
                        {% endif %}
                    {%- endif -%}
                {%- endset -%}
                {{ UI.renderHtmlProperty('orocrm.case.reporterUser.label'|trans, reporterUser) }}

                {%- set reporterContact -%}
                    {%- if entity.reporterContact -%}
                        {% if resource_granted('orocrm_contact_view') %}
                            <a href="{{ path('orocrm_contact_view', {'id': entity.reporterContact.id}) }}">
                                {{ entity.reporterContact|oro_format_name|escape }}
                            </a>
                        {% else %}
                            {{ entity.reporterContact|escape }}
                        {% endif %}
                    {%- endif -%}
                {%- endset -%}
                {{ UI.renderHtmlProperty('orocrm.case.reporterContact.label'|trans, reporterContact) }}

                {%- set reporterCustomer -%}
                    {%- if entity.reporterCustomer -%}
                        {% if resource_granted('orocrm_magento_customer_view') %}
                            <a href="{{ path('orocrm_magento_customer_view', {'id': entity.reporterCustomer.id}) }}">
                                {{ entity.reporterCustomer|oro_format_name|escape }}
                            </a>
                        {% else %}
                            {{ entity.reporterCustomer|escape }}
                        {% endif %}
                    {%- endif -%}
                {%- endset -%}
                {{ UI.renderHtmlProperty('orocrm.case.reporterCustomer.label'|trans, reporterCustomer) }}

                {%- set relatedOrder -%}
                    {%- if entity.relatedOrder -%}
                        {% if resource_granted('orocrm_magento_order_view') %}
                            <a href="{{ path('orocrm_magento_order_view', {'id': entity.relatedOrder.id}) }}">
                                {{ entity.relatedOrder|oro_format_name|escape }}
                            </a>
                        {% else %}
                            {{ entity.relatedOrder|escape }}
                        {% endif %}
                    {%- endif -%}
                {%- endset -%}
                {{ UI.renderHtmlProperty('orocrm.case.relatedOrder.label'|trans, relatedOrder) }}

                {%- set relatedCart -%}
                    {%- if entity.relatedCart -%}
                        {% if resource_granted('orocrm_magento_cart_view') %}
                            <a href="{{ path('orocrm_magento_cart_view', {'id': entity.relatedCart.id}) }}">
                                {{ entity.relatedCart|oro_format_name|escape }}
                            </a>
                        {% else %}
                            {{ entity.relatedCart|escape }}
                        {% endif %}
                    {%- endif -%}
                {%- endset -%}
                {{ UI.renderHtmlProperty('orocrm.case.relatedCart.label'|trans, relatedCart) }}

                {%- set relatedLead -%}
                    {%- if entity.relatedLead -%}
                        {% if resource_granted('orocrm_sales_lead_view') %}
                            <a href="{{ path('orocrm_sales_lead_view', {'id': entity.relatedLead.id}) }}">
                                {{ entity.relatedLead|oro_format_name|escape }}
                            </a>
                        {% else %}
                            {{ entity.relatedLead|escape }}
                        {% endif %}
                    {%- endif -%}
                {%- endset -%}
                {{ UI.renderHtmlProperty('orocrm.case.relatedLead.label'|trans, relatedLead) }}

                {%- set relatedOpportunity -%}
                    {%- if entity.relatedOpportunity -%}
                        {% if resource_granted('orocrm_sales_opportunity_view') %}
                            <a href="{{ path('orocrm_sales_opportunity_view', {'id': entity.relatedOpportunity.id}) }}">
                                {{ entity.relatedOpportunity|oro_format_name|escape }}
                            </a>
                        {% else %}
                            {{ entity.relatedOpportunity|escape }}
                        {% endif %}
                    {%- endif -%}
                {%- endset -%}
                {{ UI.renderHtmlProperty('orocrm.case.relatedOpportunity.label'|trans, relatedOpportunity) }}
            </div>
            <div class="responsive-block">
                {{ entityConfig.renderDynamicFields(entity) }}
            </div>
        </div>
    {%- endset -%}

    {% set dataBlocks = [
        {
            'title': 'orocrm.case.block.general'|trans,
            'class': 'active',
            'subblocks': [
                {'data' : [caseInformation]}
            ]
        }
    ] %}

    {% set id = 'caseeView' %}
    {% set data = { 'dataBlocks': dataBlocks } %}

    {{ parent() }}
{% endblock content_data %}
