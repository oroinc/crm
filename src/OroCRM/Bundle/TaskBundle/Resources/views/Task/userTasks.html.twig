{% if resource_granted('orocrm_task_view') %}
    {% import 'OroUIBundle::macros.html.twig' as ui %}

    {% set widgetContent %}
        {{ oro_widget_render({
            'widgetType': 'block',
            'url': path('orocrm_task_widget_user_tasks', {id: entity.id}),
            'title': 'orocrm.task.entity_plural_label'|trans,
            'cssClass': 'inner-grid',
        }) }}
    {% endset %}
    {{ ui.scrollSubblock(null, [widgetContent]) }}
{% endif %}

{% import 'OroDataGridBundle::macros.html.twig' as dataGrid %}
