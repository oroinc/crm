{% if value %}
    {% import 'OroUIBundle::macros.html.twig' as UI %}

    {% set taskId = record.getValue('id') %}
    {{ UI.clientLink({
        'dataUrl': path('orocrm_task_widget_info', {id : taskId}),
        'aCss': 'no-hash',
        'label' : value,
        'widget' : {
            'type' : 'dialog',
            'multiple' : false,
            'options' : {
                'alias': 'task_info_widget_' ~ taskId,
                'dialogOptions' : {
                    'title' : value,
                    'allowMaximize': true,
                    'allowMinimize': true,
                    'dblclick': 'maximize',
                    'maximizedHeightDecreaseBy': 'minimize-bar',
                    'width': 600
                }
            }
        },
    }) }}
{% endif %}
