<%= highlight(
    _.escape(
        _.__(
            'orocrm.magento.autocomplete.entity_number', {
                "entityName": "{{ 'orocrm.magento.order.entity_label'|trans }}",
                "entityNumber": incrementId
            }
        )
    )
) %>
