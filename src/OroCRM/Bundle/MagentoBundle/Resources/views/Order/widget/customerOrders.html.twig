{% import 'OroDataGridBundle::macros.html.twig' as dataGrid %}

<div class="widget-content">
    {{ dataGrid.renderGrid(
        'magento-customer-order-grid',
        {'customerId': customer.id, 'channelId': channel.id},
        {cssClass: 'customer_' ~ customer.id ~ '_channel_' ~ channel.id}
    ) }}
</div>
