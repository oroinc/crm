<%= highlight(
    _.escape(
        _.__(
            'oro.magento.autocomplete.entity_number', {
                "entityName": "{{ 'oro.magento.cart.entity_label'|trans }}",
                "entityNumber": originId
            }
        )
    )
) %>
