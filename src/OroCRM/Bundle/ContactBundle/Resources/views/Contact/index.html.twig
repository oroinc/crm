{% extends 'OroUIBundle:actions:index.html.twig' %}
{% import 'OroUIBundle::macros.html.twig' as UI %}
{% set gridName = 'contacts-grid' %}
{% set pageTitle = 'oro.contact.entity_plural_label'|trans %}

{% block navButtons %}
    {% include 'OroImportExportBundle:ImportExport:buttons.html.twig' with {
        entity_class: entity_class,
        exportProcessor: 'oro_contact',
        exportTemplateProcessor: 'oro_contact',
        importProcessor: 'oro_contact.add_or_replace',
        importTitle: 'oro.contact.import'|trans,
        dataGridName: gridName
    } %}

    {% if resource_granted('oro_contact_create') %}
    <div class="btn-group">
        {{ UI.addButton({
            'path' : path('oro_contact_create'),
            'entity_label': 'oro.contact.entity_label'|trans,
        }) }}
    </div>
    {% endif %}
{% endblock %}
