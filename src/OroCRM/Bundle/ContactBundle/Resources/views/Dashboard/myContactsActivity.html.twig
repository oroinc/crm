{% extends 'OroDashboardBundle:Dashboard:grid.html.twig' %}
{% set gridName = 'dashboard-my-contacts-activity-grid' %}

{% block content %}
    {{ dataGrid.renderGrid(gridName, params|default({}), {
        'routerEnabled': false,
        'enableFilters': false,
    }|merge(renderParams|default({}))) }}
{% endblock %}

{% block actions %}
    {% set actions = [{
        'url': path('orocrm_contact_index'),
        'type': 'link',
        'label': 'orocrm.dashboard.my_contacts_activity.view_all'|trans
    }] %}

    {{ parent() }}
{% endblock %}
