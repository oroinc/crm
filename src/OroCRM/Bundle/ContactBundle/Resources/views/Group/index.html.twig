{% extends 'OroUIBundle:actions:index.html.twig' %}
{% import 'OroUIBundle::macros.html.twig' as UI %}
{% set gridId = 'groups-grid' %}
{% block content %}
    {% set pageTitle = 'Contact Groups' %}
    {% set buttons = [] %}
    {% if resource_granted('orocrm_contact_group_create') %}
        {% set buttons = buttons|merge([UI.addButton({'path' : path('orocrm_contact_group_create'), 'title' : 'Create contact group', 'label' : 'Create contact group'})]) %}
    {% endif %}
    {{ parent() }}
{% endblock %}
