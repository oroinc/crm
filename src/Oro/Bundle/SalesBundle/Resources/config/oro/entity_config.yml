entity_config:
    opportunity:
        entity:
            items:
                enabled: # boolean
                    options:
                        require_schema_update: true
                        priority:           250
                        default_value:      false

    lead:
        entity:
            items:
                enabled: # boolean
                    options:
                        require_schema_update: true
                        priority:           250
                        default_value:      false
