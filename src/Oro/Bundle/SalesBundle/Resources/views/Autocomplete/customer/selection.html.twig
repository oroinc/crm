<% if (avatar.type === 'file-path') { %>
    <img src="<%= avatar.data.path %>">
<% } else if (avatar.type === 'icon') { %>
    <i class="<%= avatar.data.class %> hide-text"></i>
<% } %>
<%= _.escape(text) %>
<% if (id === null) { %>
    <span class="select2__result-entry-info">
        (<%= _.__('oro.form.add_new') %>)
    </span>
<% } %>
