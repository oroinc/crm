{% import '@OroUI/macros.html.twig' as UI %}

{{ UI.quickAccessButton({
    'dataUrl': path('oro_sales_opportunity_customer_aware_create', {
        targetId: entity.id,
        targetClass: oro_class_name(entity, true)
    }),
    'title': 'oro.sales.customer.quick_access_button.create_opportunity.label'|trans,
    'label': 'oro.sales.customer.quick_access_button.create_opportunity.label'|trans,
    'iCss': 'fa-line-chart',
    'widget': {
        'reload-grid-name': 'sales-customers-opportunities-grid',
        'options': {
            'alias': 'oro_sales_opportunity_customer_aware_create_dialog',
            'dialogOptions': {
                'title': 'oro.sales.customer.quick_access_button.create_opportunity.widget.title'|trans,
            }
        }
    }
}) }}
