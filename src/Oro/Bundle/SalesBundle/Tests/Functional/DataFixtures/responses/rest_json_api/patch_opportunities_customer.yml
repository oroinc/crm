data:
    type: opportunities
    attributes:
        name: 'test_opportunity_closed'
    relationships:
        customer:
            data:
                type: 'b2bcustomers'
                id: '<toString(@default_account_customer->getId())>'
        account:
            data:
                type: 'accounts'
                id: '<toString(@default_account->getId())>'
