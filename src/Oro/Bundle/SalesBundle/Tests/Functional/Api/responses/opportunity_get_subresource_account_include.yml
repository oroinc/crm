data:
    type: accounts
    id: '<toString(@account1->id)>'
    relationships:
        organization:
            data:
                type: organizations
                id: '<toString(@organization->id)>'
included:
    -
        type: organizations
        id: '<toString(@organization->id)>'
        attributes:
            name: OroCRM
        relationships:
            users:
                data:
                    -
                        type: users
                        id: '<toString(@user->id)>'
