{% extends '@OroDashboard/Dashboard/grid.html.twig' %}
{% import '@OroDataGrid/macros.html.twig' as dataGrid %}
{% set gridName = 'dashboard-my-contacts-activity-grid' %}

{% block content %}
    {{ dataGrid.renderGrid(gridName, params|default({}), {
        'routerEnabled': false,
        'enableFilters': false,
    }|merge(renderParams|default({}))) }}
{% endblock %}

{% block actions %}
    {% set actions = [{
        'url': path('oro_contact_index'),
        'type': 'link',
        'label': 'oro.dashboard.my_contacts_activity.view_all'|trans
    }] %}

    {{ parent() }}
{% endblock %}
