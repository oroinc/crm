{% extends "@OroAddress/widget/update.html.twig" %}
{% set addressUpdateUrl = path('oro_contact_address_update', { id: form.vars.value.id, contactId: contact.id }) %}
{% set addressCreateUrl = path('oro_contact_address_create', { contactId: contact.id }) %}
