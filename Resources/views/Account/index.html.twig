{% extends 'OroAccountBundle::layout.html.twig' %}

{% block head_script %}
    {{ parent() }}

    {% include 'OroGridBundle:Include:javascript.html.twig' with {'datagridView': datagrid, 'selector': '#accounts-grid'} %}
    {% include 'OroGridBundle:Include:stylesheet.html.twig' %}
{% endblock %}

{% block content %}
    <div class="container-fluid">
        <h1>{{ 'Accounts Overview'|trans }} <a class="btn btn-primary pull-right" href="{{ path('oro_account_create', {back: app.request.uri}) }}">{{ 'Add account'|trans }}</a></h1>
        <div id="accounts-grid"></div>
    </div>
{% endblock %}
