{% extends 'OroContactBundle::layout.html.twig' %}

{% block head_script %}
    {{ parent() }}
    {% include 'OroGridBundle:Include:javascript.html.twig' with {'datagridView': datagrid, 'selector': '#groups-grid'} %}
{% endblock %}

{% block content %}
<div class="container-fluid">
    <h1>{{ 'Groups'|trans }} <a class="btn btn-primary pull-right" href="{{ path('oro_contact_group_create') }}" id="add-new-btn">{{ 'Add new'|trans }}</a></h1>
    <div id="groups-grid"></div>
</div>
{% endblock %}
